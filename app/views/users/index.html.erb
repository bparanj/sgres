<%= form_tag users_path, method: :get do %>
  <%= text_field_tag 'term', params[:term], placeholder: 'Enter user name or surname' %>
  <%= submit_tag 'Search' %>
<% end %>

<ul>
  <%= render @users %>
</ul>